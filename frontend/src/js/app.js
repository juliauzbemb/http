import '../../../backend/app';
import '../js/http/tickets';

import ManageTickets from '../js/http/tickets';

document.addEventListener('DOMContentLoaded', () => {
    let ticketsManager = new ManageTickets();
    // ticketsManager.addTicket();
});